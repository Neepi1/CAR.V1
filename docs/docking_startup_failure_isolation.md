# 对桩观测启动失败的影响范围修复

## 现场原因

2026-09-09 17:13:56 UTC 开始等待 `/dock/target_observation`，15 秒后超时。
336L 当时仍在初始化：17:14:06 加载 preset，17:14:18 仍在设置硬件参数。
旧 common 启动器遇到该检查失败执行 `exit 1`，随后清理全部导航、API、
传感器进程，相机初始化也被打断。后面的 context-invalid 是停机后的错误，
不能用来认定相机最初崩溃。

336L 串号为 `CPC8563000LM`，独立相机串号为 `CV2T6610007C`；没有证据
表明两者争抢同一设备。此次不修改相机驱动、USB、DDS 或扫描链参数。

## 最小变更

现场 `run_common_services.sh` 只改变对桩观测启动检查的结果处理：

- 成功：记录 `docking_sensor_ready`，继续。
- 超时/失败：记录 `docking_sensor_degraded`，保留相机与观测进程，继续
  启动其他 common 服务，不因这一个传感器的初始观测缺失关闭整链。

15 秒检查、运行期健康汇总、Nav2/定位就绪条件均不放宽。不增加探针、重试、
重启策略或运动许可。精对桩入口仍需要可用且新鲜的观测；视觉控制阶段缺失
观测仍发零速。接触确认、回退、离桩原有的 BMS/里程计保护没有改变。

工作区另有“导航与 API 先启动、对桩最后启动”的未部署改动。本次未整体
部署它，而是从现场脚本快照应用上述单个分支修改；保留其他人的工作区更改。
当前工作区对应分支已具备同样的降级语义，本次补上可对现场快照回放的回归测试。

## 测试与发布

- `test_docking_startup_failure_scope.py` 执行真实脚本中的观测处理段，不调用 ROS。
  原现场版本：1 成功、3 失败；候选版本：4/4 通过，失败不能冒充 ready。
  可用 `NJRH_TEST_COMMON_SCRIPT` 指向捕获的现场脚本复测。
- 当前工作区观测范围及启动调度回归：19/19 通过。
- 健康观测器退化、Orbbec 安全约束专项：2/2 通过。
- 候选 shell 语法通过，部署前强制比对现场旧 SHA 防止覆盖并发更改。
- 只替换一个运行脚本，备份位于车端：
  `/home/nvidia/workspaces/njrh-v3/workspace1/reports/docking_startup_fix_20260909_rfV1Mi`。
- 部署脚本 SHA-256：
  `1f9e7612fd3bdd3f1652903dc12897846c1af9198f96aeb2f2988767680d24ea`。

17:21:17 UTC 启动服务，17:21:38 收到新鲜观测（年龄 0.115 秒），相机有
开流成功记录。本次现场相机在原截止内就绪，验证了正常路径；故障隔离分支
通过离线注入失败验证，没有拔掉现场相机制造故障。运行中 floor-manager
`/proc/1969420/exe` 指纹与已验收候选 `d21af096…c3ca4fe` 一致。

## 仍需现场验收

不发送运动指令。此修复不等于已经通过真实回桩、乘梯或切图测试。
完整运行状态及定位结果见 `reports/docking_startup_fix_20260909/result.md`。
