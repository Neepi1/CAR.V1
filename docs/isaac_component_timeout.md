# Isaac 组件操作超时

`src/robot_global_localization/config/global_localization.yaml` 中
`localizer_component_timeout_sec` 从 `5.0` 调整为 `15.0` 秒。

该配置是组件加载、卸载、列表与参数查询共用的最长等待时间，不是固定延时。
确认完成即继续。200 ms 单次组件列表查询、50 ms 轮询间隔、外层 30 秒
资产应用预算及 75 秒定位预算均不改变。本次只放宽已有配置，不修改判据、
失败恢复或 C++ 无配置时的默认值。

参数在 wrapper 构造组件操作适配器时读取；只同步 YAML 不会改变已有实例。
需经授权整链重启后生效，不单独重启节点、不用在线 set_parameters 伪装生效。

验证范围：YAML 解析、前后结构仅一个参数变化、源文件与安装路径哈希一致。
仍需授权重启后的静止切图验证；15 秒不保证所有组件重载都成功，也不会自动
解除上一次失败留下的状态。
