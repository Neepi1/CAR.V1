# Map poses

Owns parsing, validation, lookup, JSON formatting, and durable writing of the
selected map's `poses.yaml` semantic points.

It does not read live TF, send navigation goals, or decide which map is active.
