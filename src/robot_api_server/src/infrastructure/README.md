# Infrastructure

- `http/` owns socket transport, HTTP framing, and shared HTTP/JSON primitives.
- `process/` owns deferred execution and Linux child-process helpers.

Infrastructure modules contain no robot mission policy.
