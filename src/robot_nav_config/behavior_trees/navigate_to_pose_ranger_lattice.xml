<!--
  Keep a kinematically valid Ranger lattice path until the goal changes or
  the global costmap reports that the remaining path is no longer valid.
  This is based on Nav2 Humble's
  navigate_w_replanning_only_if_path_becomes_invalid.xml reference tree.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <PipelineSequence name="NavigateRangerLatticeWithValidPath">
      <RateController hz="1.0">
        <Fallback name="KeepValidRangerLatticePath">
          <ReactiveSequence name="CheckRangerLatticePath">
            <Inverter>
              <GlobalUpdatedGoal/>
            </Inverter>
            <IsPathValid path="{path}"/>
          </ReactiveSequence>
          <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
        </Fallback>
      </RateController>
      <FollowPath path="{path}" controller_id="FollowPath" goal_checker_id="goal_checker"/>
    </PipelineSequence>
  </BehaviorTree>
</root>
