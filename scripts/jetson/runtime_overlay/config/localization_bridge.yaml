robot_localization_bridge:
  ros__parameters:
    publish_tf: true
    map_frame: map
    odom_frame: odom
    base_frame: base_link
    jump_threshold_m: 2.0
    timeout_sec: 1.5
    localization_topic: /localization_result
    local_odom_topic: /local_state/odometry
    health_topic: /localization/health
    two_d_mode: true
