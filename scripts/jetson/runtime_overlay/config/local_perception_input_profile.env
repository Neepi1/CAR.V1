# Runtime-selected local perception input profile.
# Valid values:
#   local_branch: derive /_internal/lidar_points_local from pointcloud_axis_remap
#   trunk:        robot_local_perception subscribes directly to /lidar_points
export NJRH_LOCAL_PERCEPTION_INPUT_PROFILE=local_branch

