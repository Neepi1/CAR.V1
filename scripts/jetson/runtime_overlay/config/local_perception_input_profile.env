# Runtime-selected local perception input profile.
# Production local obstacle handling has moved to Nav2's standard /scan
# ObstacleLayer path. Keep the old PointCloud2 local-perception branch disabled.
export NJRH_LOCAL_PERCEPTION_INPUT_PROFILE=disabled
